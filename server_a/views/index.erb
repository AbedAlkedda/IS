<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Crawled Data</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
    <style>
      th {
        text-align: center;
      }
      td {
        vertical-align: middle !important;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Crawled Data</h1>
      <h2>Movie numbers using a scroller</h2>

      <table class="table table-bordered">
        <thead>
          <tr>
            <th>Title</th>
            <th>Year</th>
            <th>Rating</th>
            <th>Link</th>
            <th>Thumbnail</th>
          </tr>
        </thead>
        <tbody>
          <% @movies.each do |movie| %>
            <tr>
              <td><%= movie[:title] %></td>
              <td><%= movie[:year] %></td>
              <td><%= movie[:rating] %></td>
              <td><%= "<a href='#{movie[:link]}' target='_blank'>#{movie[:link]}</a>" %></td>
              <td><img src="<%= movie[:thumbnail] %>" alt="<%= movie[:title] %>" class="img-thumbnail"></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </body>
</html>